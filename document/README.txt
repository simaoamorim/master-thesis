Final document for this master-thesis
=====================================

Compilation
-----------
* Run `latexmk -pdf root.tex` to compile the whole document.
* Edit the file "test_chapter.tex" where the comment indicates and then run 
`latexmk -pdf test_chapter.tex` to compile just one (or more) chapters.

Cleaning
--------
Run `latexmk -C`
